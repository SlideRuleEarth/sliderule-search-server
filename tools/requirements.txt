# Build-time dependencies for the corpus builders under tools/.
# These are the *same* ones the Lambda image uses for embedding: the
# corpus builders import server/embedder.py so one code path produces
# both the committed chunk embeddings and runtime query embeddings.

onnxruntime
tokenizers
numpy
beautifulsoup4
requests

# Progress bars during corpus embedding. Optional at runtime but nice
# during multi-minute rebuilds.
tqdm

# pymupdf (AGPL-3.0): build-time only. Used by build_nsidc_corpus.py to
# extract text from PDFs. Not shipped in the Lambda image, not exposed
# over a network, not modified — so its AGPL terms stay contained to
# local / CI builds. See the full rationale in the docstring of
# tools/build_nsidc_corpus.py. If usage mode ever changes (e.g. we
# bundle it into a distributed artifact) the license picture needs
# a fresh look.
pymupdf

# Regenerating the ONNX model + tokenizer artifacts (tools/export_minilm_onnx.py)
# needs optimum + torch + transformers. Those aren't pinned here because the
# regeneration step is rare and intentionally requires a manual install:
#   .venv/bin/pip install 'sentence-transformers[onnx]'
