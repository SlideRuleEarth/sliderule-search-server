fastapi>=0.136.3
mangum>=0.21.0
pydantic>=2.13.4
# onnxruntime 1.23/1.24 terminate with "Attempt to use DefaultLogger but
# none has been registered" on Lambda arm64 because their bundled cpuinfo
# can't parse Lambda's restricted /sys/devices/system/cpu/{possible,present}
# and the resulting C++ exception escapes before the logger subsystem is
# fully initialized. Older versions handled the same sysfs shape gracefully.
# Pin until the regression is fixed upstream; track the cp313 arm64 wheel
# availability when bumping.
onnxruntime==1.26.0
tokenizers>=0.20.0
numpy>=1.24.0
